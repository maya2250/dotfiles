version: 2.1

jobs:
  build:
    docker:
      - image: maya2250/dotfiles:latest
    working_directory: ~/project
    steps:
      - checkout
      - run: sudo apt-get update -qq
      - run: ./install
      - run: ./install_brew
      - run: ./ubuntu/install_deb_packages
      - run: ./ubuntu/install_apt_packages
      - run: ./ubuntu/install_powerline
      - run: ./ubuntu/install_docker
